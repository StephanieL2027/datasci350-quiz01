cd ~/Desktop
git clone https://github.com/StephanieL2027/datasci350-quiz01.git

cd datasci350-quiz01
pwd

touch commands.txt

git checkout -b station-upgrade

mkdir results

touch results/summary.md 
echo "Analysis of the first field season." >> results/summary.md

git add results
git commit -m "Add results directory"

mv data/sensor-readings.csv data/station-data.csv

mkdir scripts/archive && cp scripts/*.py scripts/archive/

rm docs/notes.md

git add .
git commit -m "Update station files"

touch .gitignore
echo "temp/" >> .gitignore
echo "log?.txt" >> .gitignore

cat .gitignore

git add .gitignore && git commit -m "Add gitignore"

touch log{1,2,3,4}.txt

git status
Output: 
On branch station-upgrade
nothing to commit, working tree clean
Response: They do not appear because the log1.txt and all three others match the log?.txt wildcard pattern in the .gitignore which tells git to ignore, thus not appearing.

wc -l < data/station-data.csv >> results/summary.md

nano scripts/03-plot.py
git diff scripts/03-plot.py
Output:
diff --git a/scripts/03-plot.py b/scripts/03-plot.py
index 70ea841..108b791 100644
--- a/scripts/03-plot.py
+++ b/scripts/03-plot.py
@@ -1,4 +1,5 @@
-# Plot the daily temperature readings
+# Reviewed for the field season report
+#  Plot the daily temperature readings
 # Prints a simple text chart, one bar per reading.
 
 from pathlib import Path
Response: The + sign in the git diff output means the addition of a line that was not there before in the specific 03-plot.py

git add . 
git commit -m "Update analyss"

git commit --amend -m "Update analysis"

git switch main
git merge station-upgrade

Output:
2713447 (HEAD -> main, station-upgrade) Update analysis
afad8dc Add gitignore
21788f9 Update station files
4377543 Add results directory
a22b29e (origin/main, origin/HEAD) docs(readme): note committing commands.txt before task 18 and force push for bonus 2
b645f85 feat: quiz 01 starter files

git add commands.txt
git commit -m "Add command log"
git push